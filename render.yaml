services:
  - type: web
    name: tribalsetu-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn server.py:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
